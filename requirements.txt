# Phase 1 (terminal) — stdlib only

# Phase 2 (web UI)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Development / testing
httpx>=0.27.0       # required by FastAPI TestClient
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.4.0
pyright>=1.1.0
